diff --git a/packages/app/src/modules/BuiTemplateCard.tsx b/packages/app/src/modules/BuiTemplateCard.tsx
index d2d765abd7..4e30be5099 100644
--- a/packages/app/src/modules/BuiTemplateCard.tsx
+++ b/packages/app/src/modules/BuiTemplateCard.tsx
@@ -35,8 +35,6 @@ import {
 import type { TemplateCardComponentProps } from '@backstage/plugin-scaffolder-react/alpha';
 import styles from './BuiTemplateCard.module.css';
 
-const MAX_TAGS = 4;
-
 export function BuiTemplateCard(props: TemplateCardComponentProps) {
   const { template, onSelected } = props;
   const analytics = useAnalytics();
@@ -46,14 +44,21 @@ export function BuiTemplateCard(props: TemplateCardComponentProps) {
     metadata: { tags, description, name, title },
   } = template;
 
-  const visibleTags = useMemo(
-    () =>
-      Array.from(new Set([type, ...(tags ?? [])].filter(Boolean))).slice(
-        0,
-        MAX_TAGS,
-      ),
-    [type, tags],
-  );
+  const { visibleTags, remainingCount } = useMemo(() => {
+    const uniqueTags = Array.from(new Set((tags ?? []).filter(Boolean)));
+    const uniqueTagsWithoutType = uniqueTags.filter(t => t !== type);
+    const regularTags = uniqueTagsWithoutType.slice(0, 2);
+    const remainingTags = uniqueTagsWithoutType.slice(2);
+    
+    const visible = [];
+    if (type) visible.push(type);
+    visible.push(...regularTags);
+    
+    return {
+      visibleTags: visible,
+      remainingCount: remainingTags.length,
+    };
+  }, [type, tags]);
 
   const owner = getEntityRelations(template, RELATION_OWNED_BY)[0];
 
@@ -91,6 +96,9 @@ export function BuiTemplateCard(props: TemplateCardComponentProps) {
             {visibleTags.map(t => (
               <Tag key={t}>{t!}</Tag>
             ))}
+            {remainingCount > 0 && (
+              <Tag key="more">+{remainingCount} more</Tag>
+            )}
           </TagGroup>
         )}
       </Box>
